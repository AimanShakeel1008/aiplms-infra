{
  "annotations": { "list": [] },
  "panels": [
    {
      "type": "graph",
      "title": "Request latency (example)",
      "targets": [
        {
          "expr": "histogram_quantile(0.95, sum(rate(http_server_requests_seconds_bucket{service=\"auth-service\"}[5m])) by (le))",
          "format": "time_series"
        }
      ]
    }
  ],
  "schemaVersion": 27,
  "title": "Service Overview (example)"
}
